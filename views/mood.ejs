<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Mood Tracker</title>
    <style>
        h2 {
            font-family: 'Times New Roman', Times, serif;
            background-image: url('/images/headbg.png');
            color:aliceblue;
            font-style: italic;
            font-size: 50px;
        }

        body {
            background-color: aliceblue;
        }
        
        #vars {
            font-family: 'Times New Roman', Times, serif;
            font-size: 20px;
        }

    </style>
</head>

<body>
    <style>

    </style>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Mood tracker</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addMood">Add new mood</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/">View Moods</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/tasks">View Tasks</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/addTasks">Add Task</a>
                    </li>
                    <li>
                        <a onclick="return confirm('Are you sure you want to log out?')" class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <br>
        <div class="text-center"><h2>Mood details</h2></div>
        <br>
        <% if (moodTracker) { %> <!-- Check if student data exists -->
            <div id="vars">
                <p><b>Mood:</b><br> <%= moodTracker.mood %></p>
                <p><b>Emoji:</b><br> <img src = "/images/<%= moodTracker.emoji %>" width="200px"></p>
                <p><b>Mood date:</b><br> <%= new Date(moodTracker.log_date).toLocaleDateString('en-us', {month: 'long', day:'numeric', year:'numeric'}) %></p>
                <p><b>Mood description:</b><br> <%= moodTracker.moodDesc %></p> 
            </div>
        <% } else { %>
            <p>No student found.</p>
        <% } %>
        <a href ="/">Back</a>
    </div>
</body>
</html>