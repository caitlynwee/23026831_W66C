<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Mood Tracker</title>
    <style>
        h2 {
            font-family: 'Times New Roman', Times, serif;
            background-image: url('/images/headbg.png');
            color:aliceblue;
            font-style: italic;
            font-size: 50px;
        }

        th {
            font-family: 'Times New Roman', Times, serif;
            font-size: 20px;
        }

        body {
            background-color: aliceblue;
        }
        
        table {
            border: 1px solid black;
        }
        .encouragement {
            font-family: 'Times New Roman', Times, serif;
            font-size: 20px;
            background-color: rgb(151, 199, 146);
            color: aliceblue;
            text-align: center;
            padding: 10px;
            margin-top: 10px;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Mood Tracker</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addMood">Add new mood</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/">View Moods</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/addTasks">Add Task</a>
                    </li>
                    <li>
                        <a onclick="return confirm('Are you sure you want to log out?')" class="nav-link" href="/logout">Logout</a>
                    </li>
                    <li class="nav-item">
                        <form action="/deleteUser" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-primary navlink" style="color:white; border:none; background:none;" onclick="return confirm('Are you sure you want to delete your account? This action is irreversible!')">Delete account</button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <br>
        <div class="text-center">
            <h2>My Tasks</h2>
        </div>
        <br>
        <table class="table table-hover small text-center">
            <thead>
                <tr>
                    <th width="100">Task</th>
                    <th width="100">Description</th>
                    <th width="100">Date Assigned</th>
                    <th width="100">Due Date</th>
                    <th width="50">Edit</th>
                    <th width="50">Delete</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i=0; i < tasks.length; i++) { %>
                    <tr>
                        <td>
                            <a href="/task/<%= tasks[i].taskId %>">
                                <%=tasks[i].task %>
                            </a>
                        </td>
                        <td>
                            <%=tasks[i].taskdesc %>
                        </td>
                        <td>
                            <%= new Date(tasks[i].tasklogdate).toLocaleDateString('en-US', { month: 'short' , day:'2-digit',year: 'numeric' }) %>
                        </td>
                        <td>
                            <%= new Date(tasks[i].taskduedate).toLocaleDateString('en-US', { month: 'short' , day:'2-digit',year: 'numeric' }) %>
                        </td>
                        <td>
                            <a href="/editTasks/<%= tasks[i].taskId %>" class="btn btn-link">Edit</a>
                        </td>
                        <td>
                            <a href="/deleteTask/<%= tasks[i].taskId %>" class="btn btn-link" onclick="return confirm('Are you sure you want to delete this task? You might forget to do it!')">Delete</a>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
    <div class="encouragement">
        <p>"Discipline is doing what needs to be done, even if you don't to do it."</p>
    </div>
</body>
</html>