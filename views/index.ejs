<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Mood Tracker</title>
    <style>
        h2 {
            color: aliceblue;  
            font-family: 'Times New Roman', Times, serif;
            font-style: italic;
            font-size: 50px;
            background-image: url('/images/headbg.png');
        }

        th {
            font-family: 'Times New Roman', Times, serif;
            font-size: 20px;
        }

        body {
            background-color: aliceblue;
        }
        
        table {
            border: 1px solid black;
        }
        .encouragement {
            font-family: 'Times New Roman', Times, serif;
            font-size: 20px;
            background-color: rgb(151, 199, 146);
            color: aliceblue;
            text-align: center;
            padding: 10px;
            margin-top: 10px;
        }
    </style>
</head>

<body>
    <style>

    </style>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Mood Tracker</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addMood">Add new mood</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/tasks">View Tasks</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/addTasks">Add Task</a>
                    </li>
                    <li>
                        <a onclick="return confirm('Are you sure you want to log out?')" class="nav-link" href="/logout">Logout</a>
                    </li>
                    <li class="nav-item">
                        <form action="/deleteUser" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-primary navlink" style="color:white; border:none; background:none;" onclick="return confirm('Are you sure you want to delete your account? This action is irreversible!')">Delete account</button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <br>
        <div class="text-center">
            <h2>My Moods</h2>
        </div>
        <br>
        <table class="table table-hover small text-center">
            <thead>
                <tr>
                    <th width="100">Mood</th>
                    <th width="50">Emoji</th>
                    <th width="100">Description</th>
                    <th width="50">Date</th>
                    <th width="50">Edit</th>
                    <th width="50">Delete</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i=0; i < moodTracker.length; i++) { %>
                    <tr>
                        <td>
                            <a href="/mood/<%= moodTracker[i].logId %>">
                                <%=moodTracker[i].mood %>
                            </a></td>
                        <td><img src="/images/<%= moodTracker[i].emoji %>" width="50%"></td>
                        <td>
                            <%=moodTracker[i].moodDesc %>
                        </td>
                        <td>
                            <%= new Date(moodTracker[i].log_date).toLocaleDateString('en-US', { month: 'short' , day:'2-digit',year: 'numeric' }) %>
                        </td>
                        <td>
                            <a href="/editMood/<%= moodTracker[i].logId %>" class="btn btn-link">Edit</a>
                        </td>
                        <td>
                            <a href="/deleteMood/<%= moodTracker[i].logId %>" class="btn btn-link" onclick="return confirm('Are you sure you want to delete this mood? It is good to remember your feelings!')">Delete</a>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
    <div class="encouragement">
        <p>"But this is part of being human, all these emotions. There are good days and bad days. One cannot exist without the other."</p>
    </div>
</body>
</html>